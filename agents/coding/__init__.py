"""agents/coding — placeholder; to be implemented in a future sprint.

Each domain agent will follow the LLMAgent pattern:
  class CodingAgent(LLMAgent):
      name = "coding_agent"
      def _build_system_prompt(self, ...) -> str: ...
"""
