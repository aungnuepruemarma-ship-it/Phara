"""agents/chemistry — placeholder; to be implemented in a future sprint.

Each domain agent will follow the LLMAgent pattern:
  class ChemistryAgent(LLMAgent):
      name = "chemistry_agent"
      def _build_system_prompt(self, ...) -> str: ...
"""
