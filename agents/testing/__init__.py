"""agents/testing — placeholder; to be implemented in a future sprint.

Each domain agent will follow the LLMAgent pattern:
  class TestingAgent(LLMAgent):
      name = "testing_agent"
      def _build_system_prompt(self, ...) -> str: ...
"""
