"""agents/economics — placeholder; to be implemented in a future sprint.

Each domain agent will follow the LLMAgent pattern:
  class EconomicsAgent(LLMAgent):
      name = "economics_agent"
      def _build_system_prompt(self, ...) -> str: ...
"""
