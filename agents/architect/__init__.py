"""agents/architect — placeholder; to be implemented in a future sprint.

Each domain agent will follow the LLMAgent pattern:
  class ArchitectAgent(LLMAgent):
      name = "architect_agent"
      def _build_system_prompt(self, ...) -> str: ...
"""
